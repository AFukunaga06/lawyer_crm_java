# 本番MySQLプロファイル（--spring.profiles.active=mysql で有効化）
# 環境変数で上書き可能: DB_URL / DB_USER / DB_PASS
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/lawyer_crm_java?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Tokyo}
spring.datasource.username=${DB_USER:lawyer_user}
spring.datasource.password=${DB_PASS:lwcr_xzr512}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false

spring.h2.console.enabled=false
